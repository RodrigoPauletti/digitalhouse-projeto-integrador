import React from "react";

import { HeaderContent, HeaderTitle } from "./styles";

export default function Header() {
  return (
    <HeaderContent>
      <HeaderTitle>ViewGame</HeaderTitle>
    </HeaderContent>
  );
}
